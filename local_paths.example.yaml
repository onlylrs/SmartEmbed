# Developer-specific absolute paths (example template)
# Copy this file to local_paths.yaml and adjust the values for your environment.

# Base pretrained model directory (single key used across code)
base_model_path: "/absolute/path/to/jina-embeddings-v4-base"

# Optional data locations (add only what you need)
raw_data: "/absolute/path/to/data/raw"
processed_data: "/absolute/path/to/data/processed"
examples_data: "/absolute/path/to/data/examples"

# Add any other custom keys you need; access via get_path(<key>).
